#include "main.h"

/**
 * _abs - computes the absolute
 * value of an integer
 *
 * @i: the number as an integer
 *
 * Return: the absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (1);
	}
	else
	{
		return (i *-1);
	}
}
